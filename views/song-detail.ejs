<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= song.title %> - Song Detail</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container song-detail-container">
        <!-- Album and song details -->
        <div class="album-section">
            <img src="/uploads/<%= song.album_photo %>" alt="Album Photo" class="album-photo">
        </div>

        <div class="song-info-section">
            <h1 class="song-title"><%= song.title %></h1>
            <p class="lyrics"><%= song.lyrics %></p>
            <!-- Audio player -->
            <div class="audio-player">
                <audio controls>
                    <source src="/uploads/<%= song.mp3 %>" type="audio/mpeg">
                    Your browser does not support the audio element.
                </audio>
            </div>
        </div>
    </div>
    
    <!-- Back button -->
    <a href="/view-songs" class="back-button">Back to All Songs</a>
</body>
</html>
